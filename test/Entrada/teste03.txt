const {
    int constantes_=1;
}
var{
        int resultado;
    }

function int main( int param_    ){
   
var {
    int a;
    float b;
    string c;
    boolean d;
} 
    
    if ( (a > b) )  {
                for(a=1; a+11>b; a++)
                {
                    write(constantes_);
                    write(param_); 
                    write(a);
                    write(b);
                    write(c);
                    write(d);
                }
                
    }
	else {
                for(a=1; a+11>b; a++)
                {
                    read(constantes_);
                    read(param_); 
                    read(a);
                    read(b);
                    read(c);
                    read(d);
                }
        }
    

.resultado = 1;
}